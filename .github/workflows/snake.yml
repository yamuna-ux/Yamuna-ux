name: Generate Colorful Contribution Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Generate colorful snake
        uses: Platane/snk@v3
        with:
          github_user_name: yamuna-ux
          outputs: |
            dist/yamuna-snake.svg?color_snake=06B6D4&color_dots=161B22,164E63,0891B2,06B6D4,EC4899
            dist/yamuna-snake-dark.svg?color_snake=EC4899&color_dots=161B22,312E81,4338CA,06B6D4,EC4899

      - name: Publish snake
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
